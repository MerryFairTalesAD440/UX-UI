language: node_js
node_js: stable
sudo: required
dist: trusty
install:
  - npm cache verify
  - npm install
stages:
  - test
  - name: deployDev
    if: branch = develop
jobs:
  include:
    - stage: test
      script: 
      - npm run test:unit
    - stage: deployDev
      script:
      - npm run build
      - node travisCi/uploadDev.js