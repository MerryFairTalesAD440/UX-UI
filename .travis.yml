language: node_js
node_js: stable
sudo: required
dist: trusty
install:
  - npm cache verify
  - npm install
script:
  - npm run test:unit
  - npm run build
deploy:
  provider: azure_web_apps
  on: vueStarter
  verbose: true